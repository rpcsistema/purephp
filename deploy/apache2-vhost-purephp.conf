<VirtualHost *:80>
    ServerName 191.36.228.202

    # Aplicação PHP pura
    DocumentRoot /var/www/saaswl/pure-php/public
    <Directory /var/www/saaswl/pure-php/public>
        Options Indexes FollowSymLinks
        AllowOverride None
        Require all granted
    </Directory>

    # Variáveis de ambiente para conexão com banco (usadas por config.php via getenv)
    SetEnv DB_HOST 127.0.0.1
    SetEnv DB_NAME saaswl
    SetEnv DB_USER admin
    SetEnv DB_PASS 7eq8TTSOykE2xrPQt0gpztOR

    ErrorLog ${APACHE_LOG_DIR}/saaswl-purephp-error.log
    CustomLog ${APACHE_LOG_DIR}/saaswl-purephp-access.log combined
</VirtualHost>